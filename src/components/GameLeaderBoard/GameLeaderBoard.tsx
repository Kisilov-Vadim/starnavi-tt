import React, { ReactElement } from 'react'
import './GameLeaderBoard.scss';

interface Props {
  
}

export default function GameLeaderBoard({}: Props): ReactElement {

  return (
    <div className='game-board'>
      GameLeaderBoard
    </div>
  )
}
